spring:
  datasource:
    url: jdbc:mysql://localhost:3306/grupo6
    username: root
    password: 'abc'
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MySQL5Dialect
  thymeleaf:
    cache: false
